{
  "tasks": [
    {
      "n": 1,
      "requirements": [
        { "text": "AnalyticsManager.ComputeDoctorStats(): for-цикли по прийомах замінені на LINQ", "w": "critical" },
        { "text": "Перед .Select(): Appointment[] appointments = _appointments.GetAll() — масив формується один раз, не всередині SELECT", "w": "critical" },
        "Для кожного лікаря: var own = appointments.Where(a => a.DoctorId == d.Id) — фільтрація через .Where",
        "AppointmentCount: own.Count()",
        "TotalRevenue: own.Sum(a => a.GetCost())",
        { "text": "LastAppointmentDate: перевірка own.Any() перед .Max(a => a.ScheduledAt); якщо порожньо → DateTime.MinValue", "w": "critical" },
        "Результат через .Select(d => new DoctorStats(...)) або аналогічний LINQ-вираз",
        "AnalyticsManager.ComputePatientStats(): аналогічний рефакторинг для пацієнтів",
        "Сигнатури методів не змінились: IEnumerable<DoctorStats> і IEnumerable<PatientStats>",
        { "text": "Пункт '8. Аналітика' після рефакторингу: всі сортування дають ті самі результати", "w": "minor" }
      ]
    },
    {
      "n": 2,
      "requirements": [
        "Файл src/Models/SpecialityReport.cs: клас SpecialityReport",
        "SpecialityReport: Speciality Speciality { get; } — тип Speciality (enum)",
        "SpecialityReport: int DoctorCount { get; }, int AppointmentCount { get; }, decimal TotalRevenue { get; } — всі readonly",
        "SpecialityReport: конструктор що приймає всі 4 значення",
        "SpecialityReport: override ToString() — один рядок з усіма 4 значеннями"
      ]
    },
    {
      "n": 3,
      "requirements": [
        "Файл src/Managers/ReportManager.cs: клас ReportManager",
        "ReportManager конструктор: ReportManager(AppointmentManager appointments, DoctorManager doctors, PatientManager patients)",
        { "text": "GetSpecialityStats(): .GroupBy(d => d.Speciality) на лікарях → для кожної групи знайти прийоми через .Where(a => doctorIds.Contains(a.DoctorId)) → SpecialityReport → .OrderByDescending(r => r.TotalRevenue)", "w": "critical" },
        "GetSpecialityStats: doctorIds отримуються через .Select(d => d.Id).ToArray() або аналог",
        "FindBusiestDoctorName(): .OrderByDescending(d => appointments.Count(a => a.DoctorId == d.Id)) → .FirstOrDefault()?.FullName; тип повернення string?",
        { "text": "GetPatientsWithMultipleVisits(int minVisits): GroupBy(a => a.PatientId) → .Where(g => g.Count() >= minVisits) → .Join(patients, g.Key == p.Id) → p.FullName", "w": "critical" },
        "GetTopEarners(int n): обчислює виручку для кожного лікаря → .OrderByDescending(...) → .Take(n) → DoctorStats[]",
        { "text": "HasAnyUrgentAppointments(): appointments.Any(a => a is UrgentAppointment) → bool", "w": "critical" },
        { "text": "GetActiveSpecialities(): .Select(d => d.Speciality).Distinct().OrderBy(...) → IEnumerable<Speciality>", "w": "critical" },
        { "text": "GetMonthlyRevenue(): .GroupBy(a => new { a.ScheduledAt.Year, a.ScheduledAt.Month }) → (g.Key.Year, g.Key.Month, g.Sum(a => a.GetCost())) → .OrderBy(x => x.Year).ThenBy(x => x.Month)", "w": "critical" },
        "GetMonthlyRevenue: тип повернення IEnumerable<(int Year, int Month, decimal Total)> або аналог"
      ]
    },
    {
      "n": 4,
      "requirements": [
        "Clinic.cs: public ReportManager Reports { get; }",
        "Clinic конструктор: Reports = new ReportManager(Appointments, Doctors, Patients);",
        { "text": "Program.cs: у головному меню присутній пункт '11. Звіти'", "w": "minor" },
        { "text": "Program.cs: статична функція ReportsMenu(Clinic clinic) з 7 пунктами", "w": "minor" },
        { "text": "Пункт 1: GetSpecialityStats() — виводить спеціальності відсортовані за виручкою", "w": "minor" },
        { "text": "Пункт 2: FindBusiestDoctorName() — виводить ім'я або '—' якщо null", "w": "minor" },
        { "text": "Пункт 3: запит minVisits у користувача → GetPatientsWithMultipleVisits(minVisits)", "w": "minor" },
        { "text": "Пункт 4: GetTopEarners(3) — виводить 3 лікарів з нумерацією", "w": "minor" },
        { "text": "Пункт 5: HasAnyUrgentAppointments() — виводить 'Так' або 'Ні'", "w": "minor" },
        { "text": "Пункт 6: GetActiveSpecialities()", "w": "minor" },
        { "text": "Пункт 7: GetMonthlyRevenue() — формат 'YYYY/MM — NNNN.NN грн'", "w": "minor" }
      ]
    }
  ]
}
