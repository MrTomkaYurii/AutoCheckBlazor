{
  "tasks": [
    {
      "n": 1,
      "requirements": [
        "Файл src/Utils/ClinicLogger.cs: клас ClinicLogger",
        "ClinicLogger: private readonly string _logPath; конструктор ClinicLogger(string logPath = \"clinic.log\")",
        "ClinicLogger: private void Write(string level, string message): рядок формату '[yyyy-MM-dd HH:mm:ss] [level] message'",
        "Write: File.AppendAllText(_logPath, line + Environment.NewLine, Encoding.UTF8) — НЕ перезаписує, дописує в кінець",
        "ClinicLogger: public void LogInfo(string message) → Write(\"INFO\", message) або Write(\"INFO \", message)",
        "ClinicLogger: public void LogWarning(string message) та public void LogError(string message) аналогічно",
        "ClinicLogger: public string[] GetLastLines(int n) — якщо !File.Exists → Array.Empty<string>(); інакше File.ReadAllLines() + повертає останні n рядків",
        "GetLastLines: int skip = Math.Max(0, all.Length - n); — коректно якщо n > all.Length",
        "ClinicLogger: public void Clear() — видаляє або очищає файл лога",
        "ClinicLogger: public bool Exists() => File.Exists(_logPath)",
        "using System.Text; присутній (для Encoding.UTF8)",
        "Clinic.cs: властивість ClinicLogger Logger { get; }",
        "Program.cs: підменю '10. Файли' з переглядом лога і очищенням"
      ]
    },
    {
      "n": 2,
      "requirements": [
        "Файл src/Utils/ClinicExporter.cs: клас ClinicExporter",
        "ClinicExporter конструктор: ClinicExporter(Clinic clinic, string baseDir = \"reports\")",
        "PrepareDir(): Path.Combine(_baseDir, DateTime.Today.ToString(\"yyyy-MM-dd\")); Directory.CreateDirectory(dir); return dir — НЕ string concatenation",
        "ExportPatients(): string path = Path.Combine(PrepareDir(), \"patients.txt\")",
        "ExportPatients(): using StreamWriter writer = new StreamWriter(path, false, Encoding.UTF8) — using для auto-dispose",
        "ExportPatients(): записує заголовок з назвою і датою генерації, рядки з даними пацієнтів, підсумок з кількістю",
        "ExportPatients(): повертає string path до згенерованого файлу",
        "Принаймні 2 методи export (ExportPatients + ExportAppointments або ExportBilling або ExportTreatmentPlans)",
        "ExportAll(): викликає всі методи export",
        "Папка reports/yyyy-MM-dd/ створюється автоматично при першому виклику — Directory.CreateDirectory не кидає виняток якщо вже існує"
      ]
    },
    {
      "n": 3,
      "requirements": [
        "Файл src/Utils/ImportResult.cs: клас ImportResult",
        "ImportResult: private readonly List<string> _errors = new List<string>()",
        "ImportResult: public int Imported { get; private set; }",
        "ImportResult: public int Skipped { get; private set; }",
        "ImportResult: public IReadOnlyList<string> Errors => _errors",
        "ImportResult: public void AddSuccess() → Imported++",
        "ImportResult: public void AddError(int lineNumber, string reason) → Skipped++; _errors.Add(\"Рядок \" + lineNumber + \": \" + reason)",
        "ImportResult: public void Print() — виводить Imported, Skipped та список помилок",
        "Файл src/Utils/CsvImporter.cs: клас CsvImporter",
        "ImportPatients(string filePath): перевіряє File.Exists, якщо ні → result.AddError(0, ...) та return",
        "ImportPatients: File.ReadAllLines(filePath, Encoding.UTF8) з using System.Text",
        "ImportPatients: цикл від i=1 (пропуск заголовка), пропускає порожні рядки через continue",
        "ImportPatients: try/catch НАВКОЛО ОДНОГО РЯДКА циклу — не навколо всього циклу",
        "ImportPatients: line.Split(',') + перевірка parts.Length перед звертанням до parts[N]",
        "ImportPatients: DateTime.ParseExact(parts[2].Trim(), \"dd.MM.yyyy\", CultureInfo.InvariantCulture)",
        "ImportPatients: Enum.Parse(typeof(BloodType), parts[3].Trim()) для BloodType",
        "ImportPatients: result.AddSuccess() при успіху; result.AddError(i + 1, ex.Message) при помилці"
      ]
    },
    {
      "n": 4,
      "requirements": [
        "Файл src/Utils/SessionManager.cs: клас SessionManager",
        "SessionManager: private readonly string _sessionPath (дефолт \"session.dat\")",
        "SessionManager: public bool Exists() => File.Exists(_sessionPath)",
        "SessionManager.Save(Clinic clinic): using StreamWriter writer = new StreamWriter(_sessionPath, false, Encoding.UTF8)",
        "Save: записує '[PATIENTS]', потім для кожного пацієнта рядок 'FirstName,LastName,DateOfBirth,BloodType,Phone' (DateOfBirth через :dd.MM.yyyy), потім '[END]'",
        "SessionManager.Load(Clinic clinic): якщо !File.Exists → return 0",
        "Load: File.ReadAllLines; foreach рядок — секція '[PATIENTS]' через line.StartsWith(\"[\")",
        "Load: для кожного рядка секції [PATIENTS]: try { парсинг → new Patient(...) → clinic.Patients.Add(...); loaded++; } catch { }",
        "Load: catch без параметра або catch(Exception) без throw — пошкоджений рядок пропускається тихо",
        "Load: повертає int кількість завантажених пацієнтів",
        "Clinic.cs: SessionManager Session { get; }",
        "Program.cs на старті: if (clinic.Session.Exists()) → запит y/n → clinic.Session.Load(clinic)",
        "Program.cs при виході (case \"0\" або 'вихід'): запит y/n → clinic.Session.Save(clinic)"
      ]
    }
  ]
}
