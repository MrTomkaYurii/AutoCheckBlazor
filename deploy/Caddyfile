# Caddy auto-provisions and renews a real Let's Encrypt certificate for this domain
# (works with nip.io because it resolves to the server's public IP). All plain-HTTP
# traffic is redirected to HTTPS automatically.
{$APP_DOMAIN} {
	encode zstd gzip
	reverse_proxy autocheck:8080
}
