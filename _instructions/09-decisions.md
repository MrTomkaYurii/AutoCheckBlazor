# Прийняті рішення

## Архітектурні

### Чому Blazor Server а не WebAssembly
Прямий доступ до БД і сервісів з компонентів без HTTP API шару.
Простіша авторизація через стандартний ASP.NET Core middleware.

### Чому SQLite
Навчальний проект, не потребує окремого DB сервера.
При переході на production — замінити на PostgreSQL одним рядком у Program.cs.

### Чому LabStatus — int а не enum в БД
EF Core зберігає enum як int за замовчуванням.
Конвертація: `(LabStatus)sub.Status` і `(int)LabStatus.Review`.

### Чому Locked а не окремий статус "New"
Locked = "ще не здавалось". Всі лаби тепер клікабельні,
кнопка показує "Здати" а не "Заблоковано".

## Грейдинг

### Чому GitHub Actions а не локальний білд
100+ студентів одночасно знищили б сервер.
GitHub Actions ізольовані, безкоштовні для публічних репо.
Наш сервер тільки читає результат через API.

### Чому студент здає коміт а не "поточний стан гілки"
Захист від "я щось зламав після здачі".
Студент явно вибирає що здає.

### Чому не тестуємо назви методів/класів
Студенти роблять адаптації до різних доменів (клініка, готель, ресторан).
Назви можуть відрізнятись. Тестуємо поведінку (вхід → вивід).

### Чому checks.json а не тести в коді
Простіше редагувати для викладача без знання C#.
Тести на конкретні I/O пари надійніші за перевірку назв методів.

### Claude API — тільки для складних випадків
Дорого для 100 студентів × 8 задач = 800 запитів на одну лабу.
Використовувати тільки там де I/O тест неможливий.

## UI

### Чому гілки підвантажуються автоматично
Не треба вводити назву вручну — помилки друку.
GitHub API публічний, швидкий.

### Чому коміт а не просто кнопка "здати поточний стан"
Дивись вище — захист від випадкових змін після здачі.

### Чому дедлайн через datetime-local picker
Нативний браузерний пікер, без зовнішніх залежностей.
Зберігається як DateTime UTC в БД, відображається в локальному часі.

## Відомі обмеження

1. GradingService кроки — тільки заглушки (CloneStep, CheckoutStep, BuildStep, GitHubActionsStep)
2. CommitSha не зберігається в Submission (треба додати поле)
3. Черга здач не реалізована (100 одночасних = проблема)
4. GitHub token студента не зашифрований в БД
5. Тести в `content/labs/lab-01-intro/tests/` — написані, але не підключені до пайплайну

## TODO найближче

1. Додати `CommitSha` поле в `Submission`
2. Реалізувати `BuildStep` (dotnet build в temp dir)
3. Реалізувати `GitHubActionsStep` (GitHub API check-runs)
4. Підключити `checks.json` до пайплайну
5. Черга через `Channel<T>` або `BackgroundService`
